@model EmployeeManagement.Application.Positions.Models.PositionDetailsModel

@{
    ViewData["Title"] = "Position Details";
}

<div class="page-header">

    <div>
        <h1 class="page-title">
            Position Details
        </h1>

        <p class="text-muted mb-0">
            View position information and statistics.
        </p>
    </div>

    <div class="d-flex gap-2">

        <a asp-action="Edit"
           asp-route-id="@Model.Id"
           class="btn btn-primary">

            <i class="bi bi-pencil me-1"></i>
            Edit

        </a>

        <a asp-action="Index"
           class="btn btn-outline-secondary">

            <i class="bi bi-arrow-left me-1"></i>
            Back

        </a>

    </div>

</div>

<div class="row g-4 mt-2">

    <div class="col-lg-8">

        <div class="card border-0 shadow-sm">

            <div class="card-body">

                <div class="d-flex align-items-center gap-3 mb-4">

                    <div class="avatar avatar-lg bg-primary text-white">

                        <i class="bi bi-briefcase"></i>

                    </div>

                    <div>

                        <h4 class="mb-1">
                            @Model.Name
                        </h4>

                        <div class="text-muted">
                            @Model.Code
                        </div>

                    </div>

                    @if (Model.IsActive)
                    {
                        <span class="badge bg-success ms-auto">
                            Active
                        </span>
                    }
                    else
                    {
                        <span class="badge bg-secondary ms-auto">
                            Inactive
                        </span>
                    }

                </div>

                <hr />

                <div class="row g-4">

                    <div class="col-md-6">

                        <div class="text-muted small">
                            Position Code
                        </div>

                        <div class="fw-semibold">
                            @Model.Code
                        </div>

                    </div>

                    <div class="col-md-6">

                        <div class="text-muted small">
                            Position Name
                        </div>

                        <div class="fw-semibold">
                            @Model.Name
                        </div>

                    </div>

                    <div class="col-md-6">

                        <div class="text-muted small">
                            Department
                        </div>

                        <div class="fw-semibold">
                            @Model.DepartmentName
                        </div>

                    </div>

                    <div class="col-md-6">

                        <div class="text-muted small">
                            Salary Range
                        </div>

                        <div class="fw-semibold">

                            @if (Model.MinimumSalary.HasValue ||
                                Model.MaximumSalary.HasValue)
                            {
                                @($"{Model.MinimumSalary ?? 0:N2} - {Model.MaximumSalary ?? 0:N2}")
                            }
                            else
                            {
                                <span class="text-muted">
                                    Not configured
                                </span>
                            }

                        </div>

                    </div>

                    <div class="col-12">

                        <div class="text-muted small">
                            Description
                        </div>

                        <div class="mt-1">
                            @(string.IsNullOrWhiteSpace(Model.Description)
                                ? "No description provided."
                                : Model.Description)
                        </div>

                    </div>

                </div>

            </div>

        </div>

    </div>

    <div class="col-lg-4">

        <div class="card border-0 shadow-sm">

            <div class="card-body">

                <h5 class="fw-bold mb-4">
                    Position Statistics
                </h5>

                <div class="d-flex justify-content-between mb-3">

                    <span class="text-muted">
                        Employees
                    </span>

                    <span class="fw-bold">
                        @Model.EmployeeCount
                    </span>

                </div>

                <div class="d-flex justify-content-between">

                    <span class="text-muted">
                        Status
                    </span>

                    @if (Model.IsActive)
                    {
                        <span class="badge bg-success">
                            Active
                        </span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">
                            Inactive
                        </span>
                    }

                </div>

            </div>

        </div>

    </div>

</div>